{
  "version": 2,
  "builds": [
    { "src": "package.json", "use": "@vercel/static-build", "config": { "distDir": "dist/dream-website" } }
  ],
  "routes": [
    { "src": "(.*)", "dest": "/$1" }
  ]
}
